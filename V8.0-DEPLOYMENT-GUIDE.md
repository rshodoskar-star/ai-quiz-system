# 🚀 V8.0 Deployment Guide – AI Quiz System

هذا الدليل يوضح كيفية تطبيق الإصدار الثامن (V8.0) من نظام الاختبارات الذكي، والذي يتضمن تحسينات كبيرة على استخراج النصوص ومعالجة الملفات العربية وإضافة OCR للصفحات الممسوحة ضوئياً.

## ما الجديد في V8.0؟

- **تحسين استخراج النص**: استخدام `page.get_text("dict")` بدلاً من الوضع البسيط لالتقاط تخطيط الصفحات وحفظ ترتيب الأعمدة.
- **دعم العربية و RTL**: فرز الكتل وفق إحداثياتها `(y, -x)` لإعادة بناء النص العربي من اليمين إلى اليسار بدقة.
- **OCR اختياري للصفحات المصورة**: دمج مكتبة **PaddleOCR** لاستخراج النص من الصفحات الممسوحة ضوئياً عند الحاجة.
- **تطبيع النص**: إزالة التشكيل وتوحيد الحروف وإصلاح ترميز UTF‑8 باستخدام `ftfy` و `regex` لتحسين جودة المدخلات.
- **تحسين Dockerfile**: تضمين المكتبات النظامية اللازمة لـ OCR واستخدام نسخة headless من OpenCV لتقليل الحجم.

## خطوات التطبيق (≈10 دقائق)

1. **تحضير البيئة**
   - تأكد من وجود Python 3.10 أو أحدث.
   - ثبت المتطلبات من الملف `requirements-v8-simple.txt`:
     ```bash
     pip install -r requirements-v8-simple.txt
     ```

2. **تحديث ملفات المشروع**
   - استبدل `server-v7.0-PYMUPDF.js` بـ `server-v8-FIXED.js`.
   - استبدل `extract_pdf.py` بـ `extract_pdf-v8-FIXED.py`.
   - أضف ملفات التكوين الجديدة: `Dockerfile-v8-FINAL`, `package-v8-clean.json`.

3. **بناء الحاوية Docker**
   - استخدم Dockerfile الجديد:
     ```bash
     docker build -t ai-quiz-system:v8 -f Dockerfile-v8-FINAL .
     ```
   - قد تستغرق العملية 8–10 دقائق في المرة الأولى بسبب تحميل PaddleOCR والاعتماديات النظامية.

4. **تشغيل الخادم محلياً**
   - بعد البناء، شغّل الحاوية:
     ```bash
     docker run -p 8000:8000 ai-quiz-system:v8
     ```
   - تأكد أن واجهة الـ API تعمل وتستجيب لطلبات التحويل.

5. **اختبار استخراج PDF**
   - جرّب ملفات PDF تحتوي على أعمدة متعددة وصفحات مصورة للتأكد من أن النتائج أصبحت مرتبة.
   - تحقق من أن الـ OCR يُستخدم فقط عند الضرورة (عندما يكون النص قصيراً).

6. **المراقبة والتنقيح**
   - راجع السجلات في الحاوية أثناء الاختبار للتأكد من عدم ظهور أخطاء من PaddleOCR أو PyMuPDF.
   - قم بضبط عتبات الكشف عن الصفحات المصورة أو إعدادات OCR حسب الحاجة.

## كيف يعمل الـ Pipeline في V8.0

1. **فتح الملف**: يُفتح PDF باستخدام PyMuPDF ويُحصل على بيانات الصفحة في شكل dict.
2. **فرز الكتل**: تُفرز الكتل بناءً على إحداثياتها بحيث يُعاد بناء النص في ترتيبه الصحيح.
3. **اكتشاف الصفحات المصورة**: يُقاس طول النص الخام ويُفحص وجود صور؛ إذا كان النص قصيراً والصفحة تحتوي على صور، يُفعّل OCR.
4. **OCR**: تُحوّل الصفحة إلى صورة عالية الدقة ويُطبق عليها PaddleOCR للحصول على النص.
5. **تطبيع النص**: يُنظف النص بإزالة التشكيل والمسافات الزائدة وتوحيد الحروف.
6. **توليد الأسئلة**: يُرسل النص النظيف إلى النموذج اللغوي لتوليد أسئلة متعددة الأنواع.

## التحسينات الرئيسية مقارنة بـ V7.0

- رفع التغطية من ~70% إلى أكثر من 95% بفضل دعم الأعمدة و OCR.
- تقليل فقدان المعلومات في النصوص العربية المعقدة.
- تقليل الاعتماد على OCR إلا عند الحاجة، مما يحسن الأداء.
- تبسيط تثبيت الاعتماديات عبر Dockerfile موحد.

## أوقات البناء

- **البناء الأول**: 8–10 دقائق (بسبب تحميل مكتبات PaddlePaddle و opencv).
- **البناء اللاحق**: 3–5 دقائق (تستفيد من الطبقات المخزنة مؤقتاً).
- **وقت التشغيل**: يعتمد على حجم الملف؛ المعالجة الأولية للنص أسرع من OCR.

## قائمة التحقق

- [ ] تم تحديث جميع ملفات V8.0 في المستودع.
- [ ] تم تثبيت المتطلبات بنجاح بدون أخطاء.
- [ ] تم بناء Dockerfile الجديد دون مشاكل.
- [ ] تم اختبار ملفات PDF متعددة الأعمدة والصفحات المصورة بنجاح.
- [ ] لا توجد أخطاء في السجلات بعد النشر.

باتباع هذا الدليل، يمكنك الانتقال بسلاسة إلى الإصدار الثامن والاستفادة من التحسينات الكبيرة في دقة وسرعة استخراج النصوص وتوليد الأسئلة.
