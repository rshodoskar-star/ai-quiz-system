# 🎓 نظام الاختبارات الذكي - AI Quiz System

نظام متكامل يستخدم الذكاء الاصطناعي (OpenAI) لاستخراج أسئلة الاختبارات من ملفات PDF وتحويلها إلى اختبارات تفاعلية.

## ✨ المميزات

### 🤖 استخراج ذكي بالـ AI
- استخدام GPT-4 لاستخراج الأسئلة تلقائياً من PDF
- تحديد الإجابات الصحيحة تلقائياً
- استخراج أسماء الفصول
- تنظيف النص وإزالة الترويسات

### 📝 نظام اختبار متقدم
- واجهة تفاعلية سهلة الاستخدام
- دعم كامل للغة العربية (RTL)
- خيارات متعددة للإعدادات:
  - اختيار عدد الأسئلة
  - ترتيب عشوائي أو تسلسلي
  - فلترة حسب الفصل
  - خلط الخيارات
  - تغذية راجعة فورية
- شريط تقدم وعرض النتيجة المباشرة
- صفحة نتائج تفصيلية مع تقييم الأداء

### 🔒 الأمان والحماية
- عدم تخزين الملفات على الخادم
- Rate limiting لمنع الإساءة
- التحقق من نوع وحجم الملفات
- إخفاء مفاتيح API في البيئة

## 🏗️ البنية التقنية

### Backend
- **Node.js** + **Express.js**
- **OpenAI API** (GPT-4)
- **pdf-parse** لاستخراج النصوص من PDF
- **multer** لرفع الملفات
- **express-rate-limit** للحماية

### Frontend
- **HTML5** + **CSS3** + **Vanilla JavaScript**
- تصميم responsive لجميع الأجهزة
- Single Page Application (SPA)
- لا يتطلب مكتبات خارجية

## 📋 المتطلبات

- Node.js >= 16.0.0
- npm >= 8.0.0
- OpenAI API Key

## 🚀 التثبيت والتشغيل

### 1. استنساخ المشروع

```bash
git clone <repository-url>
cd ai-quiz-system
```

### 2. تثبيت الحزم

```bash
npm install
```

### 3. إعداد المتغيرات البيئية

انسخ ملف `.env.example` إلى `.env`:

```bash
cp .env.example .env
```

ثم عدّل الملف `.env` وأضف مفتاح OpenAI الخاص بك:

```env
OPENAI_API_KEY=sk-your-actual-api-key-here
OPENAI_MODEL=gpt-4o-mini
PORT=3000
MAX_PDF_SIZE_MB=10
ALLOWED_ORIGIN=http://localhost:3000
RATE_LIMIT_MAX_REQUESTS=10
```

### 4. تشغيل الخادم

**للتطوير:**
```bash
npm run dev
```

**للإنتاج:**
```bash
npm start
```

الخادم سيعمل على: `http://localhost:3000`

## 📁 هيكل المشروع

```
ai-quiz-system/
├── server.js              # Backend الرئيسي
├── package.json           # تعريف المشروع والحزم
├── .env.example          # مثال للمتغيرات البيئية
├── .env                  # المتغيرات البيئية (لا يرفع لـ git)
├── .gitignore           # ملفات مستثناة من git
├── README.md            # هذا الملف
└── public/
    └── index.html       # Frontend الكامل
```

## 🔧 API Documentation

### POST /api/quiz-from-pdf

استخراج الأسئلة من ملف PDF.

**Request:**
- Method: `POST`
- Content-Type: `multipart/form-data`
- Body: `file` (PDF file, max 10MB)

**Response (Success):**
```json
{
  "success": true,
  "totalQuestions": 25,
  "chapters": ["الفصل الأول", "الفصل الثاني"],
  "questions": [
    {
      "chapter": "الفصل الأول",
      "question": "ما هو تعريف البرمجيات؟",
      "options": ["خيار 1", "خيار 2", "خيار 3", "خيار 4"],
      "correct": 0
    }
  ],
  "processingTime": "5.23s"
}
```

**Response (Error):**
```json
{
  "success": false,
  "error": "رسالة الخطأ"
}
```

### GET /api/health

فحص حالة الخادم.

**Response:**
```json
{
  "success": true,
  "message": "Server is running",
  "timestamp": "2024-12-06T10:30:00.000Z",
  "model": "gpt-4o-mini"
}
```

## 📱 سير عمل المستخدم

1. **رفع PDF** 📄
   - الطالب يدخل الصفحة الرئيسية
   - يرفع ملف PDF يحتوي على أسئلة
   - النظام يستخرج الأسئلة تلقائياً باستخدام AI

2. **إعدادات الاختبار** ⚙️
   - اختيار عدد الأسئلة
   - اختيار ترتيب الأسئلة (تسلسلي/عشوائي)
   - فلترة حسب الفصل
   - تفعيل خلط الخيارات
   - تفعيل التغذية الراجعة

3. **الاختبار** 📝
   - عرض الأسئلة واحداً تلو الآخر
   - اختيار الإجابة
   - التغذية الراجعة الفورية (إن فُعّلت)
   - التنقل بين الأسئلة
   - شريط التقدم

4. **النتيجة** 🎉
   - عرض النسبة المئوية
   - عدد الإجابات الصحيحة/الخاطئة
   - تقييم الأداء
   - خيارات إعادة المحاولة

## 🎨 التخصيص

### تغيير الألوان

في ملف `public/index.html`، عدّل الألوان في CSS:

```css
/* اللون الرئيسي */
background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);

/* يمكنك تغييره إلى أي لون تريده */
background: linear-gradient(135deg, #your-color-1 0%, #your-color-2 100%);
```

### تغيير نموذج OpenAI

في ملف `.env`:

```env
# استخدم gpt-4 للدقة الأعلى (أغلى)
OPENAI_MODEL=gpt-4

# أو استخدم gpt-4o-mini للسرعة والتوفير (مُوصى به)
OPENAI_MODEL=gpt-4o-mini
```

### تعديل حدود الملفات

في ملف `.env`:

```env
# حجم الملف بالميجابايت
MAX_PDF_SIZE_MB=10

# عدد الطلبات في الساعة
RATE_LIMIT_MAX_REQUESTS=10
```

## 🐛 حل المشاكل الشائعة

### الخطأ: "OpenAI API key not found"

**الحل:** تأكد من وجود مفتاح API صحيح في ملف `.env`:
```env
OPENAI_API_KEY=sk-your-actual-key-here
```

### الخطأ: "فشل استخراج النص من PDF"

**الأسباب المحتملة:**
- الملف تالف أو مشفر
- PDF يحتوي على صور فقط (يحتاج OCR)
- الملف كبير جداً

**الحل:**
- جرّب ملف PDF مختلف
- تأكد من أن PDF يحتوي على نص قابل للنسخ

### الخطأ: "لم يتم العثور على أسئلة"

**الأسباب:**
- تنسيق الأسئلة غير واضح
- النص غير منظم
- AI لم يستطع التعرف على الأسئلة

**الحل:**
- تأكد من أن PDF يحتوي على أسئلة واضحة
- استخدم ملف بتنسيق أفضل

### الخطأ: "تم تجاوز الحد الأقصى للطلبات"

**الحل:** انتظر قليلاً أو زد الحد في `.env`:
```env
RATE_LIMIT_MAX_REQUESTS=20
```

## 📊 التكاليف (OpenAI API)

### نموذج gpt-4o-mini (موصى به):
- **Input:** $0.15 / 1M tokens
- **Output:** $0.60 / 1M tokens

**مثال:** ملف PDF بـ 50 سؤال ≈ 3000 tokens
- التكلفة: ~$0.001 (أقل من قرش!)

### نموذج gpt-4:
- **Input:** $5.00 / 1M tokens  
- **Output:** $15.00 / 1M tokens

**مثال:** نفس الملف
- التكلفة: ~$0.03

**💡 نصيحة:** استخدم gpt-4o-mini للتوفير، الدقة ممتازة!

## 🔐 الأمان

### ما يتم تطبيقه:
✅ مفاتيح API مخفية في البيئة  
✅ CORS محدود  
✅ Rate limiting  
✅ التحقق من نوع/حجم الملفات  
✅ عدم تخزين الملفات  
✅ معالجة في الذاكرة فقط  

### للإنتاج (Production):
- استخدم HTTPS
- فعّل Helmet.js
- أضف authentication (إن لزم)
- استخدم environment variables آمنة
- راقب الـ logs

## 🚀 النشر (Deployment)

### Heroku

```bash
# تسجيل الدخول
heroku login

# إنشاء تطبيق
heroku create your-app-name

# إضافة المتغيرات البيئية
heroku config:set OPENAI_API_KEY=your-key
heroku config:set OPENAI_MODEL=gpt-4o-mini

# النشر
git push heroku main
```

### Vercel / Netlify

هذه المنصات تدعم Serverless Functions، قد تحتاج تعديلات على الكود.

### VPS / Server

```bash
# استخدم PM2
npm install -g pm2

# تشغيل
pm2 start server.js --name "ai-quiz-system"

# حفظ
pm2 save
pm2 startup
```

## 📝 License

MIT License - استخدم المشروع بحرية!

## 🤝 المساهمة

المساهمات مرحب بها! افتح Issue أو Pull Request.

## 📧 التواصل

لأي استفسارات أو مساعدة، تواصل معنا!

---

**صُنع بـ ❤️ بواسطة RShoD & Claude**
