# 🌐 دليل نشر المشروع على aldosari.net

دليل شامل خطوة بخطوة لنشر نظام الاختبارات الذكي على دومينك الخاص.

---

## 📋 المتطلبات

- [x] مشروع ai-quiz-system جاهز
- [x] دومين aldosari.net (متوفر)
- [ ] استضافة VPS أو Heroku
- [ ] OpenAI API Key
- [ ] بطاقة دفع (للاستضافة)

---

## 🎯 الخيار 1: DigitalOcean VPS (موصى به)

### لماذا DigitalOcean؟
- ✅ سهل وبسيط
- ✅ رخيص ($6/شهر)
- ✅ أداء ممتاز
- ✅ دعم عربي
- ✅ تحكم كامل

---

## 🚀 الخطوات الكاملة

### المرحلة 1️⃣: إنشاء VPS على DigitalOcean

#### الخطوة 1: التسجيل
1. اذهب إلى: https://www.digitalocean.com/
2. سجّل حساب جديد
3. أضف بطاقة الدفع
4. **💡 استخدم كود الخصم:** احصل على $200 رصيد مجاني لمدة 60 يوم!

#### الخطوة 2: إنشاء Droplet
1. اضغط **Create** → **Droplets**
2. اختر الإعدادات التالية:

**المنطقة (Region):**
- اختر أقرب منطقة للسعودية
- **مُوصى به:** Frankfurt, Germany (سريع جداً للخليج)

**نظام التشغيل (Image):**
- اختر: **Ubuntu 22.04 LTS**

**الحجم (Size):**
- اختر: **Basic Plan**
- الخيار الأدنى: **$6/شهر**
  - 1 GB RAM
  - 1 vCPU
  - 25 GB SSD
  - 1000 GB Transfer

**Authentication:**
- اختر: **SSH Key** (أكثر أماناً)
- أو: **Password** (أسهل للمبتدئين)

**Hostname:**
- ضع: `quiz-aldosari`

3. اضغط **Create Droplet**

⏱️ **الانتظار:** 1-2 دقيقة حتى يجهز الـ Droplet

---

### المرحلة 2️⃣: الاتصال بالخادم

#### الخطوة 1: احصل على IP الخادم
بعد إنشاء Droplet، ستحصل على IP مثل: `167.99.123.45`

#### الخطوة 2: الاتصال عبر SSH

**Windows (استخدم PowerShell أو CMD):**
```bash
ssh root@167.99.123.45
```

**Mac/Linux (استخدم Terminal):**
```bash
ssh root@167.99.123.45
```

أدخل كلمة المرور (إذا اخترت Password)

---

### المرحلة 3️⃣: إعداد الخادم

بعد الاتصال، نفّذ الأوامر التالية خطوة بخطوة:

#### الخطوة 1: تحديث النظام
```bash
apt update && apt upgrade -y
```
⏱️ **الوقت:** 2-3 دقائق

#### الخطوة 2: تثبيت Node.js
```bash
# إضافة مستودع Node.js
curl -fsSL https://deb.nodesource.com/setup_18.x | bash -

# تثبيت Node.js
apt install -y nodejs

# التحقق
node --version
npm --version
```

يجب أن ترى:
```
v18.x.x
9.x.x
```

#### الخطوة 3: تثبيت Git
```bash
apt install -y git
```

#### الخطوة 4: إنشاء مجلد للمشروع
```bash
mkdir -p /var/www
cd /var/www
```

#### الخطوة 5: رفع المشروع

**الطريقة 1: عبر Git (إذا رفعته على GitHub)**
```bash
git clone https://github.com/your-username/ai-quiz-system.git
cd ai-quiz-system
```

**الطريقة 2: عبر SFTP (الأسهل)**

استخدم برنامج **FileZilla** أو **WinSCP**:

**FileZilla:**
1. حمّل من: https://filezilla-project.org/
2. افتح البرنامج
3. اتصل:
   - Host: `sftp://167.99.123.45`
   - Username: `root`
   - Password: كلمة مرورك
   - Port: `22`
4. ارفع مجلد `ai-quiz-system` إلى `/var/www/`

#### الخطوة 6: تثبيت حزم المشروع
```bash
cd /var/www/ai-quiz-system
npm install
```
⏱️ **الوقت:** 1-2 دقيقة

#### الخطوة 7: إعداد ملف .env
```bash
# انسخ المثال
cp .env.example .env

# افتح الملف للتعديل
nano .env
```

عدّل الملف:
```env
OPENAI_API_KEY=sk-proj-your-actual-key-here
OPENAI_MODEL=gpt-4o-mini
PORT=3000
NODE_ENV=production
MAX_PDF_SIZE_MB=10
ALLOWED_ORIGIN=https://aldosari.net
RATE_LIMIT_WINDOW_MS=3600000
RATE_LIMIT_MAX_REQUESTS=10
```

**مهم جداً:** غيّر `ALLOWED_ORIGIN` إلى `https://aldosari.net`

احفظ:
- اضغط `Ctrl + X`
- اضغط `Y`
- اضغط `Enter`

#### الخطوة 8: تثبيت PM2 (لإدارة العملية)
```bash
npm install -g pm2

# تشغيل المشروع
pm2 start server.js --name quiz-system

# حفظ الإعدادات
pm2 save

# تشغيل تلقائي عند إعادة التشغيل
pm2 startup
# انسخ ونفّذ الأمر الذي يظهر لك
```

#### الخطوة 9: التحقق من عمل المشروع
```bash
pm2 status
```

يجب أن ترى:
```
┌─────┬──────────────┬─────────┬─────────┐
│ id  │ name         │ status  │ cpu     │
├─────┼──────────────┼─────────┼─────────┤
│ 0   │ quiz-system  │ online  │ 0%      │
└─────┴──────────────┴─────────┴─────────┘
```

جرّب الدخول:
```bash
curl http://localhost:3000
```

إذا ظهر HTML، معناه كل شيء يعمل! ✅

---

### المرحلة 4️⃣: إعداد Nginx (Reverse Proxy)

#### الخطوة 1: تثبيت Nginx
```bash
apt install -y nginx
```

#### الخطوة 2: إنشاء ملف التكوين
```bash
nano /etc/nginx/sites-available/aldosari.net
```

الصق هذا التكوين:
```nginx
server {
    listen 80;
    server_name aldosari.net www.aldosari.net;

    location / {
        proxy_pass http://localhost:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
    }

    # حد لحجم رفع الملفات
    client_max_body_size 10M;
}
```

احفظ (`Ctrl + X` → `Y` → `Enter`)

#### الخطوة 3: تفعيل التكوين
```bash
# إنشاء رابط رمزي
ln -s /etc/nginx/sites-available/aldosari.net /etc/nginx/sites-enabled/

# حذف التكوين الافتراضي
rm /etc/nginx/sites-enabled/default

# التحقق من التكوين
nginx -t
```

يجب أن ترى:
```
nginx: configuration file /etc/nginx/nginx.conf test is successful
```

#### الخطوة 4: إعادة تشغيل Nginx
```bash
systemctl restart nginx
systemctl status nginx
```

---

### المرحلة 5️⃣: ربط الدومين (DNS Settings)

#### الخطوة 1: احصل على IP الخادم
```bash
curl ifconfig.me
```

مثلاً: `167.99.123.45`

#### الخطوة 2: إعداد DNS

اذهب إلى مزود الدومين (حيث اشتريت aldosari.net):

أضف/عدّل السجلات التالية:

| النوع | الاسم | القيمة | TTL |
|-------|-------|--------|-----|
| **A** | @ | 167.99.123.45 | 3600 |
| **A** | www | 167.99.123.45 | 3600 |

**مثال على Namecheap:**
1. اذهب إلى: Domain List → Manage
2. Advanced DNS
3. أضف سجلات A كما في الجدول أعلاه

**مثال على GoDaddy:**
1. اذهب إلى: My Products → DNS
2. أضف/عدّل سجلات A

⏱️ **الانتظار:** قد يستغرق DNS من 5 دقائق إلى 48 ساعة (عادة 15-30 دقيقة)

#### الخطوة 3: التحقق من DNS
```bash
# من حاسوبك (ليس الخادم)
ping aldosari.net
```

إذا رأيت IP الخادم، معناه DNS يعمل! ✅

---

### المرحلة 6️⃣: تفعيل HTTPS (SSL Certificate)

⚠️ **مهم جداً:** لا تبدأ هذه المرحلة إلا بعد أن يعمل DNS بشكل صحيح!

#### الخطوة 1: تثبيت Certbot
```bash
apt install -y certbot python3-certbot-nginx
```

#### الخطوة 2: الحصول على شهادة SSL
```bash
certbot --nginx -d aldosari.net -d www.aldosari.net
```

سيطلب منك:
1. **Email:** أدخل بريدك الإلكتروني
2. **Terms:** اضغط `Y` للموافقة
3. **Share email:** اختر `N`
4. **Redirect:** اختر `2` (Redirect HTTP to HTTPS) ← **مُوصى به**

⏱️ **الوقت:** 1-2 دقيقة

إذا نجح، سترى:
```
Congratulations! Your certificate has been saved...
```

#### الخطوة 3: التجديد التلقائي
```bash
# اختبار التجديد
certbot renew --dry-run
```

إذا نجح، معناه سيتم التجديد تلقائياً كل 90 يوم! ✅

---

### المرحلة 7️⃣: إعدادات الأمان الإضافية

#### الخطوة 1: إعداد Firewall
```bash
# السماح بـ SSH
ufw allow 22

# السماح بـ HTTP
ufw allow 80

# السماح بـ HTTPS
ufw allow 443

# تفعيل Firewall
ufw enable

# التحقق
ufw status
```

#### الخطوة 2: تحديث ملف .env
```bash
nano /var/www/ai-quiz-system/.env
```

تأكد من:
```env
ALLOWED_ORIGIN=https://aldosari.net
NODE_ENV=production
```

#### الخطوة 3: إعادة تشغيل التطبيق
```bash
pm2 restart quiz-system
```

---

## ✅ التحقق النهائي

### اختبار 1: افتح المتصفح
اذهب إلى: **https://aldosari.net**

يجب أن ترى:
- ✅ صفحة النظام تعمل
- ✅ قفل أخضر (HTTPS)
- ✅ لا توجد أخطاء

### اختبار 2: جرّب رفع PDF
1. ارفع ملف PDF
2. يجب أن يعمل بشكل طبيعي
3. الأسئلة تظهر

### اختبار 3: من الجوال
جرّب الدخول من الجوال:
- يجب أن يعمل بشكل مثالي
- Responsive تماماً

---

## 🎉 مبروك!

الآن موقعك **aldosari.net** شغال على الإنترنت! 🚀

---

## 📊 ملخص التكاليف الشهرية

| الخدمة | التكلفة |
|--------|---------|
| DigitalOcean VPS | $6/شهر |
| OpenAI API (متوقع) | $1-5/شهر |
| SSL Certificate | مجاني (Let's Encrypt) |
| **الإجمالي** | **$7-11/شهر** |

---

## 🔧 الصيانة الدورية

### أسبوعياً:
```bash
# مراقبة الحالة
pm2 status

# مراقبة Logs
pm2 logs quiz-system
```

### شهرياً:
```bash
# تحديث النظام
apt update && apt upgrade -y

# إعادة تشغيل
reboot
```

### عند التحديثات:
```bash
cd /var/www/ai-quiz-system
git pull  # إذا كان على GitHub
npm install
pm2 restart quiz-system
```

---

## 🆘 حل المشاكل الشائعة

### المشكلة 1: الموقع لا يفتح

**التحقق:**
```bash
# حالة Nginx
systemctl status nginx

# حالة التطبيق
pm2 status

# حالة DNS
ping aldosari.net
```

**الحل:**
```bash
# إعادة تشغيل Nginx
systemctl restart nginx

# إعادة تشغيل التطبيق
pm2 restart quiz-system
```

### المشكلة 2: خطأ SSL

**التحقق:**
```bash
certbot certificates
```

**الحل:**
```bash
# تجديد الشهادة
certbot renew --force-renewal
systemctl restart nginx
```

### المشكلة 3: OpenAI لا يعمل

**التحقق:**
```bash
# شاهد Logs
pm2 logs quiz-system --lines 50
```

**الحل:**
```bash
# تحقق من .env
cat /var/www/ai-quiz-system/.env

# تأكد من المفتاح صحيح
# ثم أعد التشغيل
pm2 restart quiz-system
```

---

## 📱 إعدادات إضافية (اختيارية)

### 1. إضافة Google Analytics
في ملف `/var/www/ai-quiz-system/public/index.html`، أضف قبل `</head>`:

```html
<!-- Google Analytics -->
<script async src="https://www.googletagmanager.com/gtag/js?id=G-XXXXXXXXXX"></script>
<script>
  window.dataLayer = window.dataLayer || [];
  function gtag(){dataLayer.push(arguments);}
  gtag('js', new Date());
  gtag('config', 'G-XXXXXXXXXX');
</script>
```

### 2. إضافة Favicon
ضع ملف `favicon.ico` في `/var/www/ai-quiz-system/public/`

### 3. رسالة صيانة (عند الحاجة)
```bash
# أوقف التطبيق
pm2 stop quiz-system

# أنشئ صفحة صيانة بسيطة
echo '<h1>الموقع تحت الصيانة</h1>' > /var/www/maintenance.html

# عدّل Nginx ليعرض صفحة الصيانة
# ثم ارجع للوضع الطبيعي بعد الانتهاء
```

---

## 🔗 روابط مفيدة

| الخدمة | الرابط |
|--------|--------|
| DigitalOcean Dashboard | https://cloud.digitalocean.com |
| SSL Labs (اختبار SSL) | https://www.ssllabs.com/ssltest/ |
| GTmetrix (اختبار السرعة) | https://gtmetrix.com |
| OpenAI Dashboard | https://platform.openai.com |

---

## ✅ قائمة التحقق النهائية

- [ ] VPS جاهز على DigitalOcean
- [ ] Node.js و npm مثبتان
- [ ] المشروع مرفوع على `/var/www/ai-quiz-system`
- [ ] ملف `.env` معدّ بشكل صحيح
- [ ] PM2 يدير التطبيق
- [ ] Nginx معدّ كـ Reverse Proxy
- [ ] DNS يشير إلى IP الخادم
- [ ] SSL/HTTPS يعمل بشكل صحيح
- [ ] Firewall مُفعّل
- [ ] الموقع يعمل على https://aldosari.net
- [ ] جرّبت رفع PDF ويعمل
- [ ] الموقع يعمل على الجوال

---

## 🎓 نصائح احترافية

1. **Backup دوري:**
   - استخدم DigitalOcean Snapshots (أسبوعياً)
   - تكلفة إضافية: ~$1/شهر

2. **Monitoring:**
   - راقب استخدام الموارد
   - راقب تكاليف OpenAI

3. **Security:**
   - غيّر كلمة مرور root
   - استخدم SSH Keys بدل Passwords
   - فعّل Two-Factor Authentication

4. **Performance:**
   - فعّل Gzip في Nginx
   - استخدم CloudFlare كـ CDN (مجاني)

---

**مبروك! موقعك الآن حي على aldosari.net! 🎊**

**شاركه مع العالم! 🌍**
