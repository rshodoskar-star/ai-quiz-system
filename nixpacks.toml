# Nixpacks configuration for V7.0 PYMUPDF
# Supports both Python and Node.js

[phases.setup]
nixPkgs = ["python310", "python310Packages.pip"]

[phases.install]
cmds = [
    "pip install -r requirements.txt",
    "npm install"
]

[start]
cmd = "npm start"
