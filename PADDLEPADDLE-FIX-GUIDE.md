# 🛠️ PaddlePaddle Fix Guide – AI Quiz System

هذه الوثيقة تشرح مشكلة شائعة عند تثبيت واستخدام مكتبة **PaddlePaddle** (الإصدار 2.5.2) ضمن بيئة بناء Docker لتشغيل **PaddleOCR**، وتعرض عدة حلول وكيفية تطبيقها.

## المشكلة

عند محاولة تثبيت `paddlepaddle==2.5.2` عبر PyPI في بيئات بناء خفيفة (مثل صور `python:3.10-slim`)، غالباً ما يفشل التثبيت بسبب اعتماديات مفقودة مثل `libgomp` أو تعارض في مكتبات النظام. هذا يؤدي إلى فشل تثبيت `PaddleOCR` وبالتالي تعطّل ميزة OCR في النظام.

## الحلول الممكنة

### 1. استخدام نسخة PyPI المحسنة (موصى بها)

- استخدم مرآة رسمية توفر نسخة من PaddlePaddle ملائمة لبيئتك. مثال:

  ```bash
  pip install paddlepaddle==2.5.2 -i https://mirror.baidu.com/pypi/simple
  pip install paddleocr
  ```

- هذا الحل يضمن تنزيل نسخة تتوافق مع البنية والاعتماديات المتوفرة في الصورة المستخدمة.

### 2. تعطيل OCR مؤقتاً

- إذا كان التعامل مع OCR غير ضروري لبعض الاستخدامات، يمكن ضبط الكود بحيث لا يُستخدم OCR عند فشل التثبيت. على سبيل المثال، استخدام العلامة `use_ocr=False` في الكود.
- هذا الخيار يقلل من الدقة في الملفات الممسوحة ولكنه يسمح للنظام بالعمل دون توقف.

### 3. الرجوع إلى الإصدار V7.0 بدون OCR

- يمكن استخدام النسخة السابقة من النظام (V7.0) التي لا تحتوي على OCR، إذا تعذّر حل المشكلة فوراً. هذا يوفر استقراراً مؤقتاً إلى حين إصلاح المشكلة.

## المقارنة بين الحلول

| الحل                       | التوافق مع الأنظمة | الدقة       | التعقيد    |
|---------------------------|--------------------|-------------|-----------|
| مرآة PyPI المحسنة        | عالٍ               | عالٍ        | متوسط     |
| تعطيل OCR                | عالٍ               | منخفضة      | منخفض     |
| الرجوع إلى V7.0          | عالٍ               | متوسطة     | منخفض     |

## خطوات التطبيق الموصى بها

1. **تحديث ملف المتطلبات**
   - في `requirements-v8-simple.txt`، تأكد من عدم تثبيت paddlepaddle افتراضياً. سنثبته يدوياً في Dockerfile.

2. **تعديل Dockerfile**
   - استخدم أوامر التثبيت التالية في Dockerfile:
     ```Dockerfile
     RUN pip install paddlepaddle==2.5.2 -i https://mirror.baidu.com/pypi/simple \
         && pip install paddleocr
     ```
   - تأكد من تثبيت الاعتماديات النظامية (`libgomp`, `libgl1`, وغيرها) قبل هذا الأمر.

3. **إعادة بناء الحاوية**
   - قم بتشغيل `docker build` مجدداً وتحقق من اكتمال التثبيت بدون أخطاء.

4. **اختبار OCR**
   - بعد البناء، شغّل النظام على ملف PDF ممسوح للتأكد من أن OCR يعمل ويعيد النص المطلوب.

## التحقق بعد النشر

- راقب سجلات التشغيل للتأكد من عدم ظهور رسائل خطأ من PaddlePaddle.
- جرّب ملفات PDF متعددة لمعرفة ما إذا كان النظام يتعامل مع الصفحات المصورة بنجاح.
- إذا ظهرت مشاكل أخرى، يمكن العودة إلى الحلول البديلة (تعطيل OCR أو استخدام V7.0) مؤقتاً.

باتباع هذه الإرشادات، سيتم حل مشكلة تثبيت PaddlePaddle في بيئات البناء الخفيفة وضمان عمل OCR بشكل صحيح في نظام الاختبارات.
