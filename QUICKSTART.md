# 🚀 دليل البدء السريع - AI Quiz System

دليل خطوة بخطوة لتشغيل النظام في دقائق!

---

## ✅ المتطلبات

قبل البدء، تأكد من تثبيت:

1. **Node.js** (الإصدار 16 أو أحدث)
   - تحميل من: https://nodejs.org/
   - للتحقق: `node --version`

2. **مفتاح OpenAI API**
   - سجّل في: https://platform.openai.com/
   - اذهب إلى API Keys
   - أنشئ مفتاح جديد

---

## 📥 الخطوة 1: تحميل المشروع

```bash
# إذا كان على GitHub
git clone <repository-url>
cd ai-quiz-system

# أو ببساطة:
# حمّل ملف ZIP وفك الضغط
```

---

## 📦 الخطوة 2: تثبيت الحزم

```bash
npm install
```

**⏱️ المدة:** 1-2 دقيقة

**الناتج المتوقع:**
```
added 150 packages in 45s
```

---

## 🔑 الخطوة 3: إعداد مفتاح OpenAI

### 3.1 انسخ ملف المثال

```bash
# Windows
copy .env.example .env

# Mac/Linux
cp .env.example .env
```

### 3.2 افتح ملف .env

استخدم أي محرر نصوص:
```bash
notepad .env        # Windows
nano .env          # Mac/Linux
code .env          # VS Code
```

### 3.3 أضف مفتاحك

عدّل هذا السطر:
```env
OPENAI_API_KEY=your_openai_api_key_here
```

إلى:
```env
OPENAI_API_KEY=sk-proj-xxxxxxxxxxxxxxxxx
```

**⚠️ مهم جداً:** احتفظ بالمفتاح سرّي ولا ترفعه على GitHub!

---

## ▶️ الخطوة 4: تشغيل النظام

```bash
npm start
```

**الناتج المتوقع:**
```
====================================
🚀 AI Quiz System Server
====================================
📡 Server running on: http://localhost:3000
🤖 AI Model: gpt-4o-mini
📁 Max PDF size: 10MB
🔒 Rate limit: 10 requests/hour
====================================
```

---

## 🌐 الخطوة 5: فتح المتصفح

افتح المتصفح واذهب إلى:
```
http://localhost:3000
```

**يجب أن ترى:** صفحة النظام الرئيسية! 🎉

---

## 🎯 الخطوة 6: جرّب النظام

### 1. ارفع ملف PDF
- اضغط على منطقة الرفع
- اختر ملف PDF يحتوي على أسئلة
- انتظر 10-30 ثانية للمعالجة

### 2. اضبط الإعدادات
- اختر عدد الأسئلة
- حدد الترتيب (عشوائي/تسلسلي)
- فعّل/عطّل الخيارات

### 3. ابدأ الاختبار
- أجب على الأسئلة
- شاهد النتيجة الفورية
- اطّلع على النتيجة النهائية

---

## ❓ حل المشاكل الشائعة

### المشكلة 1: "Cannot find module 'express'"

**الحل:**
```bash
npm install
```

### المشكلة 2: "OpenAI API key not found"

**الحل:**
- تأكد من وجود ملف `.env`
- تأكد من كتابة المفتاح بشكل صحيح
- لا تضع مسافات حول `=`

### المشكلة 3: "Port 3000 is already in use"

**الحل:**
عدّل المنفذ في `.env`:
```env
PORT=3001
```

ثم افتح: `http://localhost:3001`

### المشكلة 4: الصفحة لا تفتح

**الحل:**
1. تأكد من عمل الخادم (شاهد Console)
2. تأكد من العنوان الصحيح
3. جرّب متصفح آخر
4. امسح الـ cache: `Ctrl + Shift + R`

---

## 🎓 نصائح للاستخدام الأمثل

### 1. اختيار نموذج AI المناسب

**للتوفير والسرعة (موصى به):**
```env
OPENAI_MODEL=gpt-4o-mini
```
- ✅ سريع (5-10 ثواني)
- ✅ رخيص ($0.001 لكل ملف)
- ✅ دقة عالية

**للدقة القصوى:**
```env
OPENAI_MODEL=gpt-4
```
- ✅ دقة أعلى بقليل
- ⚠️ أبطأ (15-30 ثانية)
- ⚠️ أغلى (×30 من mini)

**القرار:** استخدم gpt-4o-mini ما لم تواجه مشاكل في الدقة!

### 2. تحسين ملفات PDF

**للحصول على أفضل نتائج:**

✅ **افعل:**
- استخدم PDF يحتوي نص قابل للنسخ
- اجعل تنسيق الأسئلة واضحاً
- أضف علامات للإجابات الصحيحة (✓)
- رتّب الأسئلة حسب الفصول

❌ **لا تفعل:**
- استخدم PDF مكون من صور فقط
- استخدم ملفات كبيرة جداً (>10MB)
- استخدم PDF مشفر أو محمي

### 3. إدارة التكاليف

**توقع التكاليف:**
- ملف صغير (10-20 سؤال): $0.0005
- ملف متوسط (30-50 سؤال): $0.001
- ملف كبير (100+ سؤال): $0.003

**نصيحة:** مع الحد الافتراضي (10 طلبات/ساعة)، التكلفة القصوى: **أقل من $0.03/ساعة**

---

## 🔄 إيقاف وإعادة التشغيل

### إيقاف الخادم

اضغط: `Ctrl + C` في نافذة Terminal

### إعادة التشغيل

```bash
npm start
```

---

## 🆘 محتاج مساعدة؟

### 1. راجع الأخطاء في Console
الأخطاء تظهر في نافذة Terminal حيث تشغل الخادم

### 2. راجع Network Tab في المتصفح
- اضغط F12 → Network
- شاهد الطلبات للـ API
- راجع Response

### 3. راجع الوثائق الكاملة
اقرأ `README.md` للمزيد من التفاصيل

---

## ✅ قائمة التحقق النهائية

قبل الاستخدام، تأكد من:

- [ ] Node.js مثبّت (v16+)
- [ ] npm مثبّت
- [ ] تم تشغيل `npm install`
- [ ] ملف `.env` موجود
- [ ] مفتاح OpenAI موجود في `.env`
- [ ] الخادم يعمل بدون أخطاء
- [ ] الصفحة تفتح في المتصفح

---

## 🎉 مبروك!

الآن أصبح لديك نظام اختبارات ذكي يعمل بكامل طاقته!

**استمتع بالاستخدام! 🚀**

---

**💡 تذكير:** هذا الإصدار الأول (MVP). يمكنك تطويره وإضافة مميزات حسب احتياجاتك!
